Import environment.yml

The 'etc' folder goes inside the env folder 'corba-contact-book'

It assumes a folder 'omniORBpy' (renamed from the downloaded and extracted windows release) is in the root of the variable %PYTHON_HOME%